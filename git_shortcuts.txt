steps:
# Pull all files from github
git pull  

# Add all files to push to github
git add .

# commit the files, after adding, enter the update name and press the following buttons to commit them

git commit

write update name in vscode: git config --global core.editor "code --wait"

# push the files to github
git push



Killing GAZEBO:
sudo pkill -f gazebo && sudo pkill -f ros && sudo pkill -f python
pkill -f gzserver
pkill -f gzclient
pkill -f gazebo

to check
ps aux | egrep "gzserver|gzclient|gazebo" | grep -v grep


move the robot with keyboard in gazebo:
ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args -r cmd_vel:=/cmd_vel


running simulation:
ros2 launch my_bot simulation.launch.py
SLAM: ros2 launch slam_toolbox online_async_launch.py use_sim_time:=true
rviz2
ros2 param set /rviz use_sim_time true
ros2 launch nav2_bringup navigation_launch.py use_sim_time:=true
